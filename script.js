const site=document.getElementById('site');const buttons=[...document.querySelectorAll('[data-theme]')];function setTheme(n){site.className='theme-'+n;buttons.forEach(b=>b.classList.toggle('active',b.dataset.theme===String(n)));location.hash='design-'+n}buttons.forEach(b=>b.addEventListener('click',()=>setTheme(b.dataset.theme)));const m=location.hash.match(/design-(\d)/);setTheme(m?m[1]:1);
